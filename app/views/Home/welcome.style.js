import { css } from '@emotion/core';
import { FONT_TINY } from 'theme';

export default css`
  margin-bottom: 30px;
  text-align: center;
  
  a {
    padding: 0 8px;
    font-size: ${FONT_TINY};
    cursor: pointer;
  }
`;